import UTILS from '../utils';
import {
  SLOT, CLASS, SCHEMA
} from '../utils/constants';

export default {
  computed: {
    SLOT: () => SLOT,
    CLASS: () => CLASS,
    UTILS: () => UTILS,
    SCHEMA: () => SCHEMA
  }
};
